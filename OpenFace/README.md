### First Step: Launch Docker

To start using OpenFace, launch the Docker container by running the following command in your terminal:

```bash
docker run -it --rm algebr/openface:latest
```

Once the container is running, execute `mainAU.py` on the same machine to see an example result. Refer to the docstring documentation in the script for more detailed information.